name: Android Development Smoke

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * 1'

jobs:
  preflight:
    runs-on: ubuntu-latest
    outputs:
      has_token: ${{ steps.check.outputs.has_token }}
    steps:
      - id: check
        env:
          GH_TOKEN: ${{ secrets.COPILOT_GITHUB_TOKEN }}
        run: |
          if [ -n "$GH_TOKEN" ]; then
            echo "has_token=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_token=false" >> "$GITHUB_OUTPUT"
            echo "COPILOT_GITHUB_TOKEN is not set; skipping android-development smoke." >> "$GITHUB_STEP_SUMMARY"
          fi

  smoke:
    needs: preflight
    if: ${{ needs.preflight.outputs.has_token == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      GH_TOKEN: ${{ secrets.COPILOT_GITHUB_TOKEN }}
      REPOS: termux,cleanarchitecture
      SCENARIOS: discovery,tasks,modernization,ui-triage
      JOBS: '2'
      RUN_ROOT: /tmp/android-development-smoke/run-${{ github.run_id }}-${{ github.run_attempt }}
      TIMEOUT_SECONDS: '240'
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'

      - name: Configure Pages
        if: always()
        uses: actions/configure-pages@v6.0.0

      - name: Install Copilot CLI
        run: npm install -g @github/copilot

      - name: Run smoke matrix
        run: ./validation/android-development/smoke.sh

      - name: Acquire site showcase data
        if: always()
        run: ./validation/android-development/site-showcase.sh

      - name: Render smoke report
        if: always()
        run: node ./scripts/render-android-smoke-report.mjs "$RUN_ROOT"

      - name: Build Pages site bundle
        if: always()
        run: |
          node ./scripts/build-pages-site.mjs \
            "$RUN_ROOT/site-dist" \
            "$RUN_ROOT" \
            "https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/data/latest.json"

      - name: Upload smoke summary
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: android-development-smoke
          path: |
            ${{ env.RUN_ROOT }}/summary.tsv
            ${{ env.RUN_ROOT }}/skill-package.txt
            ${{ env.RUN_ROOT }}/logs/*.log
            ${{ env.RUN_ROOT }}/showcase/**
            ${{ env.RUN_ROOT }}/report/**
            ${{ env.RUN_ROOT }}/site-dist/**

      - name: Upload Pages artifact
        if: always()
        uses: actions/upload-pages-artifact@v4.0.0
        with:
          path: ${{ env.RUN_ROOT }}/site-dist

  deploy_pages:
    needs:
      - preflight
      - smoke
    if: ${{ always() && needs.preflight.outputs.has_token == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5.0.0
